[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "kpcli"
version = "0.1.0"
description = "Kernel pwn helper CLI"
requires-python = ">=3.8"
dependencies = [
    "pwntools>=4.12,<5",
]

[project.scripts]
kpcli = "kpcli.cli:main"
